[pytest]
testpaths = integration_tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    requires_oci: mark test as requiring OCI credentials
    requires_multimodal: mark test as requiring multimodal model support
    asyncio: mark test as async
addopts = -v --tb=short
asyncio_mode = auto
